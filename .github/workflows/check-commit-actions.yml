name: check-commit
on:
  push:
    branches:
      - main
    paths-ignore:
      - 'README.md'
      - 'LICENSE'
  pull_request:
    branches:
      - main
    paths-ignore:
      - 'README.md'
      - 'LICENSE'
      - 'cq-cli_pyinstaller.spec'
jobs:
  run-pytest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: conda-incubator/setup-miniconda@v1
        with:
          auto-update-conda: true
          python-version: 3.8
      - run: conda init bash
      - run: source .bashrc
      - run: conda create -n cadquery
      - run: conda activate cadquery
      - run: conda info
      - run: conda install -c conda-forge -c defaults -c anaconda -c cadquery mypy pytest
      - run: pytest -v